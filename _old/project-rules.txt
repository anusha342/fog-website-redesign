# Project Rules

1. **Color Palette:**
   - White: (#FFFFFF)
   - Black: (#131313)
   - Gray: (#888888)
   - Orange: (#F05023)
   - *Note: Use exact color codes as defined in `style.css` variables (`--white`, `--dark`, `--gray`, `--accent`).*

2. Button styling with --accent color
font - 13 pixels, google sans
border radius for button should be zero

3. Button styling with transparent (hbtn-ghost) color
 font - 13 pixels, google sans
border radius for button should be zero

4. Section 
Type 1 height should be 911 pixels and width for content page should be 1440 pixels, 
Type 2 height and width should be full view port
In prompt I will specify which type to follow

5. Only if I mention it in prompt Padding for each content section should be height 96 pixels width 80 pixels

6. **Component Reuse:**
   - Reuse the footer and "Get in Touch" form from the home page (`index.html`). Do not create new versions for different pages.
7. **Typography:**
   - Headings: Clash Display (Semibold).
   - Content: Google Sans.


